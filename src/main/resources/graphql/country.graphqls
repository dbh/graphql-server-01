type Country {
    id: ID!
    name: String!
}

type Query {
    findCountryByName(name: String!): [Country]
    findAllCountries: [Country]
}

type Mutation {
    createCountry(name: String!): Country!
}